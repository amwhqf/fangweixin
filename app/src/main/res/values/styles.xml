<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="bottom_tabTextAppearance" parent="@android:style/TextAppearance">
        <item name="android:textSize">10sp</item>
    </style>

    <style name="Theme.ToolBar.Menu" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:colorBackground">@color/main_toolbar_backgroundcolor</item>
        <item name="actionOverflowMenuStyle">@style/OverflowMenuStyle</item>
        <item name="android:textSize">12sp</item>
        <!-- 用于替换菜单的三个小白点，换成自已的图片-->
        <!--<item name="actionOverflowButtonStyle">@style/ActionButton.Overflow.Menu</item>-->

        <!--  Item 分割线-->
        <item name="android:actionOverflowButtonStyle">@style/ActionButton.Overflow.Menu</item>
        <item name="android:dropDownListViewStyle">@style/dropDownStyle</item>
    </style>

    <style name="OverflowMenuStyle" parent="Widget.AppCompat.Light.PopupMenu.Overflow">
        <item name="overlapAnchor">false</item>
    </style>

    <style name="ActionButton.Overflow.Menu" parent="android:style/Widget.Holo.Light.ActionButton.Overflow">
        <item name="android:src">@drawable/icon_add_pressed</item>
    </style>


    <!--用于修改popmenu的分割线-->
    <style name="dropDownStyle" parent="android:style/Widget.Holo.ListView.DropDown">
        <item name="android:dividerHeight">0.5dp</item>
        <item name="android:divider">#000000</item>
    </style>

</resources>
